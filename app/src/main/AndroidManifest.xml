<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.environmentalcampaign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnvironmentalCampaign">
        <activity android:name=".PointMarket"></activity>
        <activity android:name=".MyPage" ></activity>
        <activity android:name=".FeedPage"></activity>
        <activity android:name=".CertificationPage" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookMark"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".SearchPage"></activity>
        <activity android:name=".SetUpCampaignPage"></activity>
    </application>

</manifest>