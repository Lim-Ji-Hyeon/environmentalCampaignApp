<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.environmentalcampaign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EnvironmentalCampaign">
        <activity android:name=".CpMakelist" />
        <activity android:name=".CampaignSituation"></activity>
        <activity android:name=".CampaignInformation" />
        <activity android:name=".PointMarket" />
        <activity android:name=".MyPage" ></activity>
        <activity android:name=".FeedPage" />
        <activity android:name=".CertificationPage" />
        <activity android:name=".BookMark" />
        <activity android:name=".HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".SearchPage" />
        <activity android:name=".SetUpCampaignPage" />
    </application>

</manifest>